{% macro btn_icon(type, dir) %}
    <span class='m{{dir}}-2'>
        <i class='fa fa-{{type}}'></i>
    </span>
{% endmacro %}

{% macro sort_th_a(sort_by, url) %}
    <a href="{{url}}?sort_by={{sort_by}}&order={% if request.args.get('sort_by')==sort_by and request.args.get('order', 'desc')=='desc' %}asc{% else %}desc{% endif %}">
        {% if request.args.get('sort_by')==sort_by%}
            {% if request.args.get('order')=='asc' %}
                {{btn_icon('sort-up', '')}}
            {% else %}
                {{btn_icon('sort-down', '')}}
            {% endif %}
        {% else %}
                {{btn_icon('sort', '')}}
        {% endif %}
    </a>
{% endmacro %}

{% macro pagination(items, url) %}
    <nav>
        <ul class="pagination justify-content-center">
            {% if items.has_prev %}
                <li class="page-item"><a class="page-link" href="{{url}}?page={{items.prev_num}}{% if request.args.get('sort_by')%}&sort_by={{request.args.get('sort_by')}}{% endif %}{% if request.args.get('order')%}&order={{request.args.get('order')}}{% endif %}">
                    {{ btn_icon('arrow-left', 'r')}}
                    Fyrri
                </a></li>
            {% else %}
                <li class="page-item disabled"><a class="page-link" href="#">
                    {{ btn_icon('arrow-left', 'r')}}
                    Fyrri
                </a></li>
            {% endif %}
            <li class='page-item active'>
                <span class='page-link'>{{items.page}} af {{items.pages}}</span>
            </li>
            {% if items.has_next %}
                <li class="page-item"> <a class="page-link" href="{{url}}?page={{items.next_num}}{% if request.args.get('sort_by')%}&sort_by={{request.args.get('sort_by')}}{% endif %}{% if request.args.get('order')%}&order={{request.args.get('order')}}{% endif %}">
                    Næsta
                    {{ btn_icon('arrow-right', 'l')}}
                </a></li>
            {% else %}
                <li class="page-item disabled"><a class="page-link" href="#">
                    Næsta
                    {{ btn_icon('arrow-right', 'l')}}
                </a></li>
            {% endif %}

        </ul>
    </nav>
{% endmacro %}

{% macro recording_analysis(recording) %}
        {% if recording.analysis %}
            {% if recording.analysis == 'ok' %}
                <span class='text-success'>
                    <i class='fa fa-thumbs-up mr-2'></i>
                    Góð
                </span>
            {% elif recording.analysis == 'high' %}
                <span class='text-danger'>
                    <i class='fa fa-thumbs-down mr-2'></i>
                    Of há
                </span>
            {% elif recording.analysis == 'low' %}
                <span class='text-danger'>
                    <i class='fa fa-thumbs-down mr-2'></i>
                    Of lág
                </span>
            {% endif %}
        {% else %}
            <span class='text-warning'>
                <i class='fa fa-question-circle mr-2'></i>
                Ekki greint
            </span>
        {% endif %}
{% endmacro %}

{% macro recording_verification(recording) %}
    {% if recording.is_secondarily_verified %}
        <span class='text-success'>
            <i class='fa fa-certificate mr-2'></i>
            Tvígreind
        </span>
    {% elif recording.is_verified %}
        <span class='text-success'>
            Eingreind
        </span>
    {% else %}
        <span class='text-warning'>
            <i class='fa fa-question-circle mr-2'></i>
            Engin greining
        </span>
    {% endif %}
{% endmacro %}


{% macro pron_spans(prons) %}
    {% for pron in prons %}
        <span class='mr-1 mt-1 px-2 rounded bg-secondary' style="white-space: nowrap; display:inline-block;">{{pron | safe}}</span>
    {% endfor %}
{% endmacro %}

{% macro default(val, def) %}
    {% if val %}
        {{val}}
    {% elif def %}
        {{val}}
    {% else %}
        Ekki tilgreint
    {% endif %}
{% endmacro %}

{% macro no_results(title, text, link, link_text, icon, type) %}
    <div class='jumbotron'>
        <h3 class='font-weight-bold'>
            <span class='text-{% if type %}{{type}}{% else %}warning{% endif %}'>
                <i class='fa fa-{% if icon %}{{icon}}{% else %}exclamation-triangle{% endif%}'></i>
            </span>
            {{title}}
        </h3>
        <p>{{text}}</p>
        {% if link %}
            <a href={{link}} class='btn btn-secondary text-success float-sm-right'>
                {{link_text}}
                {{btn_icon('fa fa-arrow-right', '')}}
            </a>
        {% endif %}
    </div>
{% endmacro %}


{% macro verifier_icon(classes, color, size) %}
    <i class='{{classes}} fa-{{size}}' style='color: {{color}}'></i>
{% endmacro %}

{% macro icon_card(icon, current_user)%}
    <div class="card {% if current_user.progression.is_icon_equipped(icon)%}border border-success{% endif %}" style='width: 100%'>
        <div class="card-body d-flex justify-content-between flex-column">
            <div>
                <p class="card-title text-center font-weight-bold">{{icon.title}}</p>
                <p class='text-center'>{{verifier_icon(icon.fa_id, icon.color, '3x')}}</p>
                <p class='text-center'>{{icon.description}}</p>
            </div>
            <div>
                {% if not current_user.progression.owns_icon(icon) %}
                    <a href="{{url_for('icon_buy', icon_id=icon.id, user_id=current_user.id)}}" class="btn btn-warning btn-sm rounded-pill text-center float-right">
                        <span class="mr-2 ml-2 font-weight-bold">{{icon.price}}</span>
                        <i class="fas fa-dollar-sign mr-2"></i>
                    </a>
                {% else %}
                    {% if not current_user.progression.is_icon_equipped(icon) %}
                        <a href="{{url_for('icon_equip', icon_id=icon.id, user_id=current_user.id)}}" class="btn btn-secondary text-success btn-sm rounded-pill text-center float-right">
                            <span class="mr-2 ml-2 font-weight-bold">Velja</span>
                            <i class="fas fa-check mr-2"></i>
                        </a>
                    {% else %}
                        <a class="btn btn-secondary btn-sm rounded-pill text-center float-right">
                            <span class="mr-2 ml-2 font-weight-bold">Valinn</span>
                        </a>
                    {% endif %}
                {% endif %}

                {% if current_user.is_admin() %}
                    <a href="{{icon.edit_url}}" class="mr-1 btn btn-secondary text-warning btn-sm rounded-pill text-center float-right">
                        <span class="mr-2 ml-2 font-weight-bold">
                            <i class='fa fa-edit'></i>
                        </span>
                    </a>
                {% endif %}
            </div>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item {% if icon.rarity == 1 %}bg-primary{% elif icon.rarity==2 %}bg-danger{% elif icon.rarity==3%}bg-warning{% else %}bg-info{% endif %}">
                <span>
                    {% if icon.rarity == 1 %}
                        Sjaldgæft
                    {% elif icon.rarity==2 %}
                        Epískt
                    {% elif icon.rarity==3%}
                        Goðsagnakennt
                    {% else %}
                        Algengt
                    {% endif %}
                </span>
                    <span class='float-right'>
                    {% for i in range(4) %}
                        {% if i < icon.rarity + 1 %}
                            <i class='fa fa-xs fa-circle'></i>
                        {% else %}
                            <i class='far fa-xs fa-circle'></i>
                        {% endif %}
                    {% endfor %}
                </span>
            </li>
        </ul>
    </div>
{% endmacro %}

{% macro title_card(title, current_user)%}
    <div class="card {% if current_user.progression.is_title_equipped(title)%}border border-success{% endif %}" style='width: 100%'>
        <div class="card-body d-flex justify-content-between flex-column">
            <div>
                <p class="card-title text-center font-weight-bold h4">{{title.title}}</p>
                <p class='text-center'><i class="fas {{fa_id}} fa-3x text-success"></i></p>
                <p class='text-center'>{{title.description}}</p>
            </div>
            <div>
                {% if not current_user.progression.owns_title(title) %}
                    <a href="{{url_for('title_buy', title_id=title.id, user_id=current_user.id)}}" class="btn btn-warning btn-sm rounded-pill text-center float-right">
                        <span class="mr-2 ml-2 font-weight-bold">{{title.price}}</span>
                        <i class="fas fa-dollar-sign mr-2"></i>
                    </a>
                {% else %}
                    {% if not current_user.progression.is_title_equipped(title) %}
                        <a href="{{url_for('title_equip', title_id=title.id, user_id=current_user.id)}}" class="btn btn-secondary text-success btn-sm rounded-pill text-center float-right">
                            <span class="mr-2 ml-2 font-weight-bold">Velja</span>
                            <i class="fas fa-check mr-2"></i>
                        </a>
                    {% else %}
                        <a class="btn btn-secondary btn-sm rounded-pill text-center float-right">
                            <span class="mr-2 ml-2 font-weight-bold">Valinn</span>
                        </a>
                    {% endif %}
                {% endif %}


                {% if current_user.is_admin() %}
                    <a href="{{title.edit_url}}" class="mr-1 btn btn-secondary text-warning btn-sm rounded-pill text-center float-right">
                        <span class="mr-2 ml-2 font-weight-bold">
                            <i class='fa fa-edit'></i>
                        </span>
                    </a>
                {% endif %}
            </div>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item {% if title.rarity == 1 %}bg-primary{% elif title.rarity==2 %}bg-danger{% elif title.rarity==3%}bg-warning{% else %}bg-info{% endif %}">
                <span>
                    {% if title.rarity == 1 %}
                        Sjaldgæft
                    {% elif title.rarity==2 %}
                        Epískt
                    {% elif title.rarity==3%}
                        Goðsagnakennt
                    {% else %}
                        Algengt
                    {% endif %}
                </span>
                    <span class='float-right'>
                    {% for i in range(4) %}
                        {% if i < title.rarity + 1 %}
                            <i class='fa fa-xs fa-circle'></i>
                        {% else %}
                            <i class='far fa-xs fa-circle'></i>
                        {% endif %}
                    {% endfor %}
                </span>
            </li>
        </ul>
    </div>
{% endmacro %}

{% macro slogan_card(quote, current_user)%}
    <div class="card {% if current_user.progression.is_quote_equipped(quote)%}border border-success{% endif %}" style="width:100%">
        <div class="card-body d-flex justify-content-between flex-column">
            <div>
                <p class="card-title text-center">{{quote.quote}}</p>
            </div>
            <div>
                {% if not current_user.progression.owns_quote(quote) %}
                    <a href="{{url_for('quote_buy', quote_id=quote.id, user_id=current_user.id)}}" class="btn btn-warning btn-sm rounded-pill text-center float-right">
                        <span class="mr-2 ml-2 font-weight-bold">{{quote.price}}</span>
                        <i class="fas fa-dollar-sign mr-2"></i>
                    </a>
                {% else %}
                    {% if not current_user.progression.is_quote_equipped(quote) %}
                        <a href="{{url_for('quote_equip', quote_id=quote.id, user_id=current_user.id)}}" class="btn btn-secondary text-success btn-sm rounded-pill text-center float-right">
                            <span class="mr-2 ml-2 font-weight-bold">Velja</span>
                            <i class="fas fa-check mr-2"></i>
                        </a>
                    {% else %}
                        <a class="btn btn-secondary btn-sm rounded-pill text-center float-right">
                            <span class="mr-2 ml-2 font-weight-bold">Valinn</span>
                        </a>
                    {% endif %}
                {% endif %}

                {% if current_user.is_admin() %}
                    <a href="{{quote.edit_url}}" class="mr-1 btn btn-secondary text-warning btn-sm rounded-pill text-center float-right">
                        <span class="mr-2 ml-2 font-weight-bold">
                            <i class='fa fa-edit'></i>
                        </span>
                    </a>
                {% endif %}
            </div>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item {% if quote.rarity == 1 %}bg-primary{% elif quote.rarity==2 %}bg-danger{% elif quote.rarity==3%}bg-warning{% else %}bg-info{% endif %}">
                <span>
                    {% if quote.rarity == 1 %}
                        Sjaldgæft
                    {% elif quote.rarity==2 %}
                        Epískt
                    {% elif quote.rarity==3%}
                        Goðsagnakennt
                    {% else %}
                        Algengt
                    {% endif %}
                </span>
                    <span class='float-right'>
                    {% for i in range(4) %}
                        {% if i < quote.rarity + 1 %}
                            <i class='fa fa-xs fa-circle'></i>
                        {% else %}
                            <i class='far fa-xs fa-circle'></i>
                        {% endif %}
                    {% endfor %}
                </span>
            </li>
        </ul>
    </div>
{% endmacro %}

{% macro loot_card(loot, current_user)%}
    <div class="card {% if current_user.progression.is_icon_equipped(loot)%}border border-success{% endif %}" style='width: 100%; border-bottom: 10px solid #563d7c;'>
        <div class="card-body d-flex justify-content-between flex-column">
            <div>
                <p class="card-title text-center font-weight-bold">{{loot.title}}</p>
                <p class='text-center'>{{verifier_icon('fas fa-question', '#563d7c', '5x')}}</p>
            </div>
            <div>
                {% if not current_user.progression.owns_icon(loot) %}
                    <a href="{{url_for('icon_buy', icon_id=loot.id, user_id=current_user.id)}}" class="btn btn-warning btn-sm rounded-pill text-center float-right">
                        <span class="mr-2 ml-2 font-weight-bold">{{loot.price}}</span>
                        <i class="fas fa-dollar-sign mr-2"></i>
                    </a>
                {% else %}
                    {% if not current_user.progression.is_icon_equipped(loot) %}
                        <a href="{{url_for('icon_equip', icon_id=loot.id, user_id=current_user.id)}}" class="btn btn-secondary text-success btn-sm rounded-pill text-center float-right">
                            <span class="mr-2 ml-2 font-weight-bold">Velja</span>
                            <i class="fas fa-check mr-2"></i>
                        </a>
                    {% else %}
                        <a class="btn btn-secondary btn-sm rounded-pill text-center float-right">
                            <span class="mr-2 ml-2 font-weight-bold">Valinn</span>
                        </a>
                    {% endif %}
                {% endif %}

                {% if current_user.is_admin() %}
                    <a href="{{loot.edit_url}}" class="mr-1 btn btn-secondary text-warning btn-sm rounded-pill text-center float-right">
                        <span class="mr-2 ml-2 font-weight-bold">
                            <i class='fa fa-edit'></i>
                        </span>
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
{% endmacro %}

{% macro verifier_card(current_user) %}
    <div class='card'>
        <div class='card-body'>
            <p class='text-center'>
                {{verifier_icon(current_user.progression.equipped_icon.fa_id, current_user.progression.equipped_icon.color, '4x')}}
            </p>
            <p class='font-weight-bold text-center'>{{current_user.progression.equipped_title.title}} {{current_user.name}}</p>
            <p class='font-italic text-center'>{{current_user.progression.equipped_quote.quote}}</p>
            <p class='text-center mb-0'>
                <span class='text-warning mr-4'>
                    {{current_user.progression.lobe_coins}}
                    <i class='fa fa-coins ml-1'></i>
                </span>
                <span class='text-danger'>
                    {{current_user.progression.experience}}
                    <i class='fa fa-gem ml-1'></i>
                </span>
            </p>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item">
                <span>Greinandi</span>
                <span class='text-warning float-right'>
                    {% for key, _ in config.ECONOMY.achievements.verification.items() %}
                        {% if current_user.progression.verification_level > key|int %}
                            <i class='fa fa-xs fa-circle'></i>
                        {% else %}
                            <i class='far fa-xs fa-circle'></i>
                        {% endif %}
                    {% endfor %}
                </span>
            </li>
            <li class="list-group-item">
                <span>Spæjari</span>
                <span class='text-warning float-right'>
                    {% for key, _ in config.ECONOMY.achievements.spy.items() %}
                        {% if current_user.progression.spy_level > key|int %}
                            <i class='fa fa-xs fa-circle'></i>
                        {% else %}
                            <i class='far fa-xs fa-circle'></i>
                        {% endif %}
                    {% endfor %}
                </span>
            </li>
            <li class="list-group-item">
                <span>Garpur</span>
                <span class='text-warning float-right'>
                    {% for key, _ in config.ECONOMY.achievements.streak.items() %}
                        {% if current_user.progression.streak_level > key|int %}
                            <i class='fa fa-xs fa-circle'></i>
                        {% else %}
                            <i class='far fa-xs fa-circle'></i>
                        {% endif %}
                    {% endfor %}
                </span>
            </li>
        </ul>
    </div>
{% endmacro %}